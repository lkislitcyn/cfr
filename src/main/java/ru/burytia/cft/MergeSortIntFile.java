package ru.burytia.cft;

import java.io.*;
import java.util.ArrayList;
import java.util.Scanner;

public class MergeSortIntFile {
    void margeSortIntFile(FileReader fileReader1, FileReader fileReader2, FileWriter fileWriter) {
        ArrayList<Integer> arrayList = new ArrayList<>();
        try (Scanner scanner = new Scanner(fileReader1); Scanner scanner1 = new Scanner(fileReader2);
        ) {

        }
    }
}
